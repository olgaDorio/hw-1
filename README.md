# Как запустить

```sh
npm install
```

## Сборка фронта (в директорию `/docs`)
```sh
npm run start
npm run build
```

## Запуск сервер на порту, указанном в `process.env.PORT` (или 8000)
```sh
npm run server
```
